# DataR0w — interface (Phase 7)

React + Vite + Plotly. Backend : `python -m avsim.api`.

```bash
npm install
npm run dev
```

Proxy Vite : `/api` → `http://127.0.0.1:8000`.

Principes : badge **Simulé** partout côté moteur ; classes `2x`…`8x` en
**Bêta — non calibrée** jusqu'à `test_class_scaling.py`.
